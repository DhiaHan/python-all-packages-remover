# python-all-packages-remover
Remove all pip/pip3 installed packages at once, with one bash script.


# How to run

## Remove packages installed with pip3:
```
bash pip3remover.sh
```
## Remove packages installed with pip:
```
bash pipremover.sh
```
